# Отчёт по вопросам к 6 лабораторной работе

**Тема: современные веб-технологии и инструменты разработки**
**Основано на материале 7 лекции**

---

## 1. В чём разница между ECMA-262, ECMAScript и JavaScript?

| **Термин** | **Описание**                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ECMA-262**     | **Официальный** **стандарт** **, утверждённый организацией** **Ecma International** **. Это документ, который описывает синтаксис, семантику и поведение языка.**                                                                                                                                                                           |
| **ECMAScript**   | **Спецификация языка программирования** **, определённая в стандарте ECMA-262. Это "техническое имя" языка — то, что на самом деле стандартизировано.**                                                                                                                                                                                               |
| **JavaScript**   | **Реализация**языка ECMAScript, созданная компанией Netscape и впоследствии развитая в браузерах (V8 в Chrome, SpiderMonkey в Firefox и др.). На практике термины "JavaScript" и "ECMAScript" часто используются как синонимы, но технически JavaScript — это**одна из реализаций**стандарта ECMAScript. |

> **💡 ** **Аналогия** **: **
>
> * **ECMA-262 — ГОСТ **
> * **ECMAScript — описание двигателя по ГОСТу **
> * **JavaScript — конкретный мотор от Toyota, соответствующий этому описанию**

---

## 2. Что такое полифилл, транспайлинг, Babel, SWC и как связаны между собой?

### 🔹 Полифилл (polyfill)

* **Это ** **код** **, который эмулирует современные API в старых средах.**
* **Пример: **`Promise` не поддерживается в IE11 → подключаем полифилл `core-js` или `es6-promise`.

### 🔹 Транспайлинг (transpiling)

* **Процесс ****преобразования** кода на одном языке (или его новой версии) в **эквивалентный код**на том же или более старом языке.
* **Пример: преобразование **`const x = () => {}` (ES2015+) → `var x = function() {}` (ES5).

### 🔹 Babel

* **Популярный транспайлер** JavaScript.
* **Поддерживает плагины для полифиллов (**`@babel/preset-env` + `core-js`).
* **Позволяет писать современный JS и запускать его в старых браузерах.**

### 🔹 SWC (Speedy Web Compiler)

* **Современная альтернатива Babel** **, написанная на Rust.**
* **В разы быстрее** (до 20–100×).
* **Поддерживает транспайлинг, минификацию, сборку.**
* **Используется в таких инструментах, как ** **Next.js** **, ** **Deno** **, ****Vite** (опционально).

### 🔗 Связь между ними

* **Транспайлинг** — общая задача.
* **Babel и SWC** — инструменты для транспайлинга.
* **Полифиллы** часто используются  **вместе с транспайлингом** **: Babel/SWC преобразуют синтаксис, а **`core-js` добавляет недостающие методы (`Array.from`, `Object.assign`, и т.д.).

---

## 3. Что такое bundler, webpack, vite и как связаны между собой?

### 🔹 Bundler (сборщик)

* **Инструмент, который ****объединяет** множество JS-модулей, CSS, изображений и других ресурсов в **один или несколько файлов** для браузера.
* **Решает проблемы:**
  * **Множество **`<script>` → один бандл
  * **Современные модули (ESM) → поддержка в старых браузерах**
  * **Оптимизация (минификация, tree-shaking)**

### 🔹 Webpack

* **Классический bundler** **, появился ~2012 г.**
* **Очень гибкий: всё настраивается через **`webpack.config.js`.
* **Поддерживает ** **hot module replacement (HMR)** **, code splitting, loaders/plugins.**
* **Минус** **: медленная сборка (из-за архитектуры на JS).**

### 🔹 Vite

* **Современный инструмент** (создан Эваном Ю, автором Vue.js).
* **Не использует традиционный bundler на этапе разработки.**
* **Использует ****нативные ES-модули в браузере** + **esbuild/SWC** для транспайлинга.
* **Плюсы** **:**
* **Мгновенный запуск dev-сервера (независимо от размера проекта)**
* **Встроенная поддержка TypeScript, JSX, CSS Modules и т.д.**
* **Для продакшена использует ****Rollup** (а не Webpack).

### 🔗 Связь

* **Bundler** — общее понятие.
* **Webpack и Vite** — конкретные реализации.
* **Vite — ** **эволюция подхода** **: отказ от полной сборки в dev-режиме в пользу нативных возможностей браузера.**

---

## 4. Что такое WebSocket и чем он отличается от Server-Sent Events (SSE)?

| **Характеристика**            | **WebSocket**                                                                        | **Server-Sent Events (SSE)**                                            |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- |
| **Направление**                  | **Двусторонний**(full-duplex): клиент ↔ сервер              | **Односторонний** **: сервер → клиент** |
| **Протокол**                        | `ws://`или `wss://`(отдельный от HTTP)                                   | **Использует****обычный HTTP/HTTPS**                   |
| **Данные**                            | **Бинарные и текстовые**                                           | **Только****текст (UTF-8)**                                  |
| **Переподключение**          | **Не встроено (реализуется вручную)**                    | **Автоматическое**переподключение          |
| **Поддержка браузерами** | **Хорошая (все современные)**                                   | **Хорошая, кроме****IE и старых Edge**               |
| **Использование**              | **Чаты, игры, реальное взаимодействие**                | **Ленты, уведомления, логгирование**        |
| **Сложность**                      | **Выше (нужен отдельный серверный обработчик)** | **Низкая (обычный HTTP-роут в Express)**              |

> **💡 ****Когда что использовать?**
>
> * **Нужен ****чат** или **онлайн-игра** → WebSocket
> * **Нужны ****уведомления от сервера** (новости, статус задачи) → SSE

---

## 5. TypeScript: преимущества и недостатки по сравнению с языками с типизацией "из коробки" (например, Java, C#, Rust)

### ✅ Преимущества TypeScript

1. **Постепенное внедрение типов**
   Можно начать с `.js` → переименовать в `.ts` → добавлять типы постепенно.
2. **Идеальная интеграция с JavaScript**
   Любой JS-код — валидный TS. Можно использовать существующие библиотеки (с `@types/...`).
3. **Развитая система типов**
   * **Утилитарные типы (**`Partial`, `Pick`, `Record`)
   * **Условные типы**
   * **Mapped types**
   * **Infer**
4. **Поддержка современных фич**
   Декораторы, JSX, async/await — всё работает "из коробки".
5. **Отличная поддержка в редакторах**
   VS Code (от Microsoft) имеет встроенную поддержку TS.

---

### ❌ Недостатки по сравнению с "нативными" типизированными языками

| **Недостаток**                                           | **Пояснение**                                                                                                                                                                                                      |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Типы исчезают при компиляции**          | **Нет runtime-типов → нельзя делать рефлексию, проверки типов в рантайме без дополнительных библиотек (например,**`io-ts`,`zod`). |
| **Меньшая строгость по умолчанию**      | `any`тип, неявные приведения,`undefined`могут пройти, если не включить строгие флаги (`strict: true`).                                                             |
| **Нет настоящих ошибок компиляции**    | **TS — надмножество JS, поэтому****всегда можно обойти типизацию**(через `@ts-ignore`,`as any`).                                                                     |
| **Производительность не улучшается** | **В отличие от Rust/C++, TS не влияет на скорость выполнения — это только инструмент разработки.**                                                      |
| **Сложность конфигурации**                    | `tsconfig.json`может быть очень сложным для больших проектов.                                                                                                                            |

> **💡 ** **Вывод** **:**
> TypeScript — **инструмент повышения надёжности и читаемости** JavaScript-кода, но **не замена**таким языкам, как Java или Rust. Он оптимален для веб-разработки, где гибкость важнее строгой безопасности типов.
>
