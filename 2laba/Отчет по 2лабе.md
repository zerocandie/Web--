

### 🔹 **Базовые типы**

```ts
const userName: string = "Alice";
```
> Объявляется константа `userName` типа `string` со значением `"Alice"`.

```ts
const userAge: number = 28;
```
> Константа `userAge` типа `number` — хранит целое число 28.

```ts
const isActive: boolean = true;
```
> Логическое значение: `true` или `false`. Здесь — `true`.

```ts
const favoriteNumbers: number[] = [3, 7, 42];
```
> Массив чисел. Тип `number[]` означает «массив, состоящий из элементов типа `number`».

```ts
const tupleExample: [string, number] = ["points", 10];
```
> **Кортеж (tuple)**: массив фиксированной длины и строгих типов. Первый элемент — строка, второй — число.

```ts
const bigInteger: bigint = 9007199254740991n;
```
> Тип `bigint` для безопасной работы с целыми числами больше `Number.MAX_SAFE_INTEGER`. Суффикс `n` обязателен.

```ts
const uniqueId: symbol = Symbol("id");
``>
> Тип `symbol` — уникальный и неизменяемый примитив. `Symbol("id")` создаёт уникальный идентификатор.

```ts
const optionalValue: string | undefined = undefined;
```
> Переменная может быть либо строкой, либо `undefined`. Здесь — `undefined`.

```ts
const nullableValue: string | null = null;
```
> Может быть строкой или `null`. Здесь — `null`.

```ts
function greet(name: string): string {
  return `Привет, ${name}!`;
}
```
> Функция `greet`, принимающая строку и возвращающая строку.

---

### 🔹 **Интерфейсы и типы**

```ts
interface User {
  id: number;
  name: string;
  email?: string;
}
```
> Определяется интерфейс `User` с обязательными полями `id` и `name`, и **опциональным** `email` (знак `?`).

```ts
interface Admin extends User {
  role: "admin";
  permissions: string[];
}
```
> Интерфейс `Admin` **наследует** все поля от `User` и добавляет `role` (строго `"admin"`) и массив `permissions`.

```ts
const admin: Admin = {
  id: 1,
  name: "Bob",
  role: "admin",
  permissions: ["manage-users", "view-reports"],
};
```
> Создаётся объект, соответствующий интерфейсу `Admin`. Поле `email` опущено — оно опционально в `User`.

```ts
type ApiResponse<T> = {
  data: T;
  error?: string;
};
```
> **Generic-тип** `ApiResponse`. `T` — placeholder для любого типа. Объект содержит `data` типа `T` и опциональную ошибку.

```ts
const adminResponse: ApiResponse<Admin> = {
  data: admin,
};
```
> Конкретизация `ApiResponse` с `T = Admin`. Объект содержит `data: Admin`, ошибки нет — допустимо.

---

### 🔹 **keyof, typeof, in, as (динамическая типизация)**

```ts
const settings = {
  theme: "dark",
  language: "ru",
  notifications: true,
};
```
> Обычный JavaScript-объект с настройками.

```ts
type Settings = typeof settings;
```
> `typeof` в TypeScript **выводит тип объекта**. `Settings` становится алиасом типа:
> ```ts
> { theme: string; language: string; notifications: boolean }
> ```

```ts
type SettingKey = keyof Settings;
```
> `keyof` создаёт **объединение строковых литералов** — всех ключей объекта:
> ```ts
> type SettingKey = "theme" | "language" | "notifications";
> ```

```ts
const updateSetting = (key: SettingKey, value: Settings[SettingKey]) => {
  settings[key] = value;
};
```
> Функция принимает:
> - `key` — только один из разрешённых ключей (`"theme"`, `"language"`, и т.д.),
> - `value` — только значение, **соответствующее типу этого ключа** (например, для `"theme"` — только `string`).
>
> Это **типобезопасное обновление объекта**.

---

### 🔹 **Utility Types (встроенные вспомогательные типы)**

```ts
type Article = {
  id: string;
  title: string;
  body: string;
  tags: string[];
  publishedAt?: Date;
};
```
> Базовый тип статьи.

```ts
type ArticleDraft = Partial<Article>;
```
> `Partial<T>` делает **все поля T опциональными**. Полезно для черновиков:
> ```ts
> { id?: string; title?: string; ... }
> ```

```ts
type ArticlePreview = Pick<Article, "id" | "title" | "tags">;
```
> `Pick<T, K>` выбирает **только указанные поля** из `T`.

```ts
type ArticleWithoutBody = Omit<Article, "body">;
```
> `Omit<T, K>` — наоборот, **исключает указанные поля**.

```ts
type ArticleTagMap = Record<string, ArticlePreview>;
```
> `Record<K, T>` создаёт объект, где **все ключи — строки**, а **все значения — типа `ArticlePreview`**.
> Например: `{ "typescript": { id: "...", title: "...", tags: [...] } }`.

```ts
const draft: ArticleDraft = {
  title: "Новый пост про TypeScript",
};
```
> Черновик: можно указывать только часть полей.

```ts
const preview: ArticlePreview = {
  id: "1",
  title: "Знакомство с TypeScript",
  tags: ["typescript", "basics"],
};
```
> Предпросмотр: только нужные поля.

```ts
const withoutBody: ArticleWithoutBody = {
  id: "2",
  title: "Гайд по типам",
  tags: ["typescript"],
};
```
> Статья без тела — `body` исключён.

```ts
const tagMap: ArticleTagMap = {
  typescript: preview,
};
```
> Объект, сопоставляющий теги с предпросмотрами статей.

```ts
const loadArticle = (id: string) => ({
  id,
  title: "Заголовок",
  body: "Содержимое",
  tags: ["typescript"],
});
```
> Функция, возвращающая объект статьи (без `publishedAt`).

```ts
type LoadArticleResult = ReturnType<typeof loadArticle>;
```
> `ReturnType<T>` извлекает **возвращаемый тип функции**. Здесь:
> ```ts
> { id: string; title: string; body: string; tags: string[] }
> ```

```ts
const result: LoadArticleResult = loadArticle("3");
```
> Переменная строго типизирована под результат функции — TypeScript проверит соответствие.

---

### 💡 Заключение

Этот фрагмент демонстрирует **основные и продвинутые возможности TypeScript**:
- Примитивные типы и составные (`tuple`, `array`)
- Интерфейсы и наследование
- Generics и generic-утилиты (`ApiResponse<T>`)
- Вывод типов (`typeof`, `keyof`, индексные типы `T[K]`)
- Встроенные утилиты (`Partial`, `Pick`, `Omit`, `Record`, `ReturnType`)

Всё это позволяет писать **гибкий, самодокументируемый и типобезопасный код**.

Если хочешь — могу сделать краткую шпаргалку по этим утилитам или объяснить любую часть глубже.